<h3 class="page-header">
  All Users
</h3>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.email, user_url(user) %> <%= link_to "Edit", edit_user_path(user) %> <%= link_to "Delete", user_path(user), method: :delete, data: { confirm: 'Are you sure?' } %></li>
  <% end %>
</ul>

